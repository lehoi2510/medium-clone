export const PAGINATION_CONSTANTS = {
  DEFAULT_LIMIT: 20,
  DEFAULT_OFFSET: 0,
  MAX_LIMIT: 100,
  MIN_LIMIT: 1,
  MIN_OFFSET: 0,
} as const;

export const ARTICLE_CONSTANTS = {
  DEFAULT_PAGE: 1,
} as const;
